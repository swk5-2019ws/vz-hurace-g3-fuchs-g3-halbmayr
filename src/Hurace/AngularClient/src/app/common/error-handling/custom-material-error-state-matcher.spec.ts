import { CustomMaterialErrorStateMatcher } from './custom-material-error-state-matcher';

describe('CustomMaterialErrorStateMatcher', () => {
  it('should create an instance', () => {
    expect(new CustomMaterialErrorStateMatcher()).toBeTruthy();
  });
});
