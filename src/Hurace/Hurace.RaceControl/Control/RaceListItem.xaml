<UserControl x:Class="Hurace.RaceControl.Control.RaceListItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Hurace.RaceControl.Control"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="150">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0"
               Content="{Binding Race.Venue.Name}"/>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal">

            <Label Content="{Binding Season.Name}"/>

            <Label Content="{Binding Race.Date}"/>

        </StackPanel>
    </Grid>
</UserControl>
