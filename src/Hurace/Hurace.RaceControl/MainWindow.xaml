<Window x:Class="Hurace.RaceControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:controls="clr-namespace:Hurace.RaceControl.Control"
        xmlns:viewModels="clr-namespace:Hurace.RaceControl.ViewModels"
        Title="Race Control" Height="450" MinHeight="400" Width="800" MinWidth="650">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition MinWidth="230" MaxWidth="250"/>
            <ColumnDefinition MinWidth="500"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="35"/>
            </Grid.RowDefinitions>

            <ListBox Grid.Row="0"
                     BorderThickness="0"
                     ItemsSource="{Binding RaceListItemViewModels}">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type viewModels:RaceListItemViewModel}">
                        <controls:RaceListItem/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            
            <Button Grid.Row="1"/>
        </Grid>
        
        <GridSplitter Width="2"
                      Grid.Column="0"
                      HorizontalAlignment="Right"
                      VerticalAlignment="Stretch"
                      Background="DarkGray"/>

        <Grid Grid.Column="1">
            
        </Grid>
    </Grid>
</Window>
